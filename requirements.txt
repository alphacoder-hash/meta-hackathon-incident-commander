# IncidentCommander OpenEnv — Python dependencies
# Used as fallback when uv is not available.
# Primary dependency management is via pyproject.toml + uv sync.

# Core OpenEnv runtime (provides FastAPI server + HTTP client types + WebSocket)
openenv-core[core]>=0.2.2

# LLM inference
openai>=1.0.0

# HTTP client
httpx>=0.27.0
requests>=2.32.3

# YAML parsing
pyyaml>=6.0.2

# Dev / testing
pytest>=8.0.0
